// Calculo Potencia (^)
let base = 2;
let expoente = 4;
let resultado = Math.pow(base, expoente); // o math.pow calcua a potencia sozinho, parecido com o math.sqrt
console.log("Resultado da Potência: ",resultado);